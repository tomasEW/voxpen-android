<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/keyboard_background"
    android:padding="4dp">

    <!-- Candidate area -->
    <LinearLayout
        android:id="@+id/candidate_bar"
        android:layout_width="match_parent"
        android:layout_height="128dp"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:visibility="gone">

        <!-- Translation indicator (tappable to cycle target language, × to close) -->
        <LinearLayout
            android:id="@+id/translation_indicator_row"
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:background="?android:attr/selectableItemBackground"
            android:visibility="gone">

            <TextView
                android:id="@+id/translation_label"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/key_text"
                android:textSize="13sp"
                android:ellipsize="end"
                android:maxLines="1" />

            <ImageButton
                android:id="@+id/btn_translation_close"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/translation_close"
                android:src="@drawable/ic_close"
                android:scaleType="centerInside" />
        </LinearLayout>

        <!-- Single-line status (recording/processing/error) -->
        <LinearLayout
            android:id="@+id/candidate_status_row"
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/candidate_progress"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginEnd="8dp"
                android:visibility="gone"
                style="?android:attr/progressBarStyleSmall" />

            <TextView
                android:id="@+id/candidate_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/key_text"
                android:textSize="15sp"
                android:ellipsize="end"
                android:maxLines="2" />

            <ImageButton
                android:id="@+id/btn_copy_status"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="4dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/copy_to_clipboard"
                android:src="@drawable/ic_content_copy"
                android:scaleType="centerInside"
                android:visibility="gone" />
        </LinearLayout>

        <!-- Original STT result row (always shown while refining and after refinement) -->
        <TextView
            android:id="@+id/candidate_original"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="38dp"
            android:gravity="center_vertical"
            android:padding="6dp"
            android:textColor="@color/key_text"
            android:textSize="14sp"
            android:ellipsize="end"
            android:maxLines="2"
            android:background="?android:attr/selectableItemBackground"
            android:visibility="gone" />

        <!-- Divider between original and refined -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#33000000"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp" />

        <!-- Refined text row (tappable) -->
        <LinearLayout
            android:id="@+id/candidate_refined_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="40dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="6dp"
            android:background="?android:attr/selectableItemBackground"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/refine_progress"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginEnd="6dp"
                android:visibility="gone"
                style="?android:attr/progressBarStyleSmall" />

            <TextView
                android:id="@+id/candidate_refined"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/key_text"
                android:textSize="15sp"
                android:textStyle="bold"
                android:ellipsize="end"
                android:maxLines="2" />

            <ImageButton
                android:id="@+id/btn_copy_refined"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="4dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/copy_to_clipboard"
                android:src="@drawable/ic_content_copy"
                android:scaleType="centerInside"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>

    <!-- Key row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center">

        <ImageButton
            android:id="@+id/btn_switch"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/key_background"
            android:contentDescription="@string/keyboard_switch"
            android:src="@drawable/ic_language"
            android:layout_margin="2dp" />

        <ImageButton
            android:id="@+id/btn_backspace"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/key_background"
            android:contentDescription="@string/keyboard_backspace"
            android:src="@drawable/ic_backspace"
            android:layout_margin="2dp" />

        <ImageButton
            android:id="@+id/btn_mic"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="@color/mic_idle"
            android:contentDescription="@string/keyboard_record"
            android:src="@drawable/ic_mic"
            android:layout_margin="2dp" />

        <TextView
            android:id="@+id/btn_tone"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/key_background"
            android:gravity="center"
            android:textSize="20sp"
            android:text="\uD83D\uDCAC"
            android:layout_margin="2dp" />

        <ImageButton
            android:id="@+id/btn_enter"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/key_background"
            android:contentDescription="@string/keyboard_enter"
            android:src="@drawable/ic_keyboard_return"
            android:layout_margin="2dp" />

        <ImageButton
            android:id="@+id/btn_settings"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/key_background"
            android:contentDescription="@string/keyboard_settings"
            android:src="@drawable/ic_settings"
            android:layout_margin="2dp" />
    </LinearLayout>
</LinearLayout>
